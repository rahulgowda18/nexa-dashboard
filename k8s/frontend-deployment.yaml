apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: nexa
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: rahulgowda526/nexa-frontend:latest
          ports:
            - containerPort: 80